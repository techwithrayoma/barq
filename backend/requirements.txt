# FastAPI and web server
fastapi                   # FastAPI web framework for building APIs
uvicorn[standard]==0.37.0 # ASGI server to run FastAPI apps (with standard extras for performance)

# Data validation and settings
pydantic==2.12.2          # Data validation and settings management
pydantic-settings==2.11.0 # Advanced configuration management for environment variables

# Google / YouTube API
google-api-python-client==2.96.0 # Official Google API client for accessing YouTube data

# Database / ORM
SQLAlchemy==2.0.44        # ORM for database access (supports sync & async)

# Database drivers
# Async drivers (use these for async database access)
asyncpg==0.30.0           # PostgreSQL async driver
#aiomysql                 # MySQL async driver (uncomment if using MySQL)
#aiosqlite                # SQLite async driver (uncomment if using SQLite)

# Sync drivers (use these for sync database access)
psycopg2-binary==2.9.11  # PostgreSQL sync driver
#pymysql                  # MySQL sync driver (uncomment if using MySQL)
#sqlite3                  # SQLite sync driver (built-in, no need to install)

# Database migrations
alembic==1.17.0           # Database migration tool

# HTTP client for async requests
aiohttp==3.13.1           # Async HTTP client (used for YouTube API requests)

# Rate limiting for FastAPI
slowapi==0.1.9            # Rate limiting middleware for FastAPI

# Testing
pytest==7.4.2              # Python testing framework
pytest-asyncio==0.22.0     # Async support for pytest (for testing async FastAPI/DB code)

# HuggingFace transformers and dependencies
# Force NumPy <2 for PyTorch compatibility
numpy<2
transformers==4.41.0
torch==2.2.0   # Required backend for transformers